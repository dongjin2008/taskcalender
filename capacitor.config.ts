import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saeronam.saerotask',
  appName: 'saerotask',
  webDir: 'out/grade/11/11-1'
};

export default config;
